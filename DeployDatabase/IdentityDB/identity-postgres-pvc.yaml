apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: identity-postgresql-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-storage-retain
  resources:
    requests:
      storage: 3Gi