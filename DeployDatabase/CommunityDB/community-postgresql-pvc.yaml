apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: community-postgresql-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-storage-retain
  resources:
    requests:
      storage: 3Gi